allprojects {
  ext {
    encoding = 'UTF-8'
  }

  tasks.withType(JavaCompile) {
    options.encoding = encoding
    options.debug = true
  }
// TODO:  compileJava.options.debugOptions.debugLevel = "source,lines,vars"

  sourceCompatibility = '1.8'
}
